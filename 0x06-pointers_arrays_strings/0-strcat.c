#include "main.h"

/**
 * _strcat - a function that concatenates two strings
 * @dest: input value (s1)
 * @src: input value (s2)
 *
 * Return: void
 */
char *_strcat(char *dest, char *src)
{
	int j = 0, k = 0, d = 0;

	while (dest[j] != '\0')
	{
		j++;
	}
	while (src[k] != '\0')
	{
		k++;
	}
	while (d <= c)
	{
		dest[j] = src[n];
		d++;
		j++;
	}
	return (dest);
}
